"use client";

export default function DoughInventoryPage({ params }) {
  const { id } = params;

  return (
    <section className="p-6">
      <h1 className="text-2xl font-bold text-pink-600 mb-4">Dough Inventory</h1>
    </section>
  );
}
